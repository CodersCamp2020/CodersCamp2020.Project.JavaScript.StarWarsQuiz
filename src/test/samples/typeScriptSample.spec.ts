import {describe, it} from "mocha";
import {expect, assert} from 'chai';

describe('TypeScript sample test', () => {

    context("given sample", () => {

        describe("when sample", () => {

            it("then sample", () => {
                expect(true).to.be.equals(true)
            })

        });

    });


});
